name: "example-cron-action"
on:
  schedule:
    - cron:  '*/2 * * * *'    # At every 15th minute

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - name: Print Hello World
      run: echo "Hola Mundo"
